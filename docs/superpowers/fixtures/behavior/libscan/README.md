# 픽스처 — 케이스 H (docs-organize 2차 훑기)

라우트 3개와 공유 모듈 2개.

- `src/lib/notify/order-notify.ts` — 라우트 2개가 import. **도메인 규칙**을 담는다
  (누가·언제 알림을 받는지 결정). → 기능 행으로 올라와야 한다.
- `src/lib/utils/format.ts` — 라우트 3개가 import. 범용 포맷터. → 올라오면 안 된다.

빈도만 보면 format이 더 많이 쓰인다. 빈도로 판정하면 이 픽스처가 잡는다.
2026-08-27 필드 검증에서 실증된 사각지대(라우트 없는 공유 인프라)를 고정한다.
