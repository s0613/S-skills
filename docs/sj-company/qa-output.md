# QA Output — 로그인 페이지에 '비밀번호 찾기' 링크 추가
> 생성일: 2026-05-15

## 테스트 케이스

### 기능 검증
- [ ] TC-01: 로그인 페이지에 '비밀번호를 잊으셨나요?' 링크가 렌더링되는가
- [ ] TC-02: 링크 클릭 시 `/forgot-password`로 이동하는가
- [ ] TC-03: 기존 이메일 입력 필드가 정상 동작하는가
- [ ] TC-04: 기존 비밀번호 입력 필드가 정상 동작하는가
- [ ] TC-05: 기존 로그인 버튼 제출이 정상 동작하는가

### 디자인 검증
- [ ] TC-06: 링크가 비밀번호 필드 우측 상단에 위치하는가
- [ ] TC-07: 링크 색상이 `#8a8f98` (subtle)이고 로그인 버튼보다 시각적 위계가 낮은가
- [ ] TC-08: 호버 시 색상이 `#d0d6e0`으로 변경되고 밑줄이 표시되는가

## 엣지 케이스
- `/forgot-password` 라우트가 없는 경우 → 404 오류 발생 (TC-02 FAIL)
- 모바일 뷰포트(<375px)에서 `flex justify-between` 레이아웃이 겹치는 경우
- CSS 모듈 대신 Tailwind 사용 프로젝트에서 스타일 적용 미스
- `<a>` 태그 대신 `<Link>` 미사용 시 Next.js 클라이언트 라우팅 미적용 (하드 리로드 발생)

## 판정: CONDITIONAL

구현 명세(dev-output.md)는 PM 요구사항과 Design 명세를 모두 충족한다.
단, 실제 적용 전 아래 2가지 조건 확인이 필요하다:

1. `/forgot-password` 라우트 존재 여부 확인 → 없으면 별도 태스크로 생성 후 배포
2. 프로젝트의 CSS 방식(모듈/Tailwind/styled-components) 확인 후 `.forgotLink` 스타일 방식 조정

두 조건 충족 시 **PASS**.

## 발견된 이슈
- **이슈-01 (MEDIUM)**: `/forgot-password` 라우트 존재 여부 미확인 → Dev가 라우트 파일 확인 필요
- **이슈-02 (LOW)**: CSS 방식 명시 없음 → Dev가 프로젝트 스타일 패턴 확인 후 `.forgotLink` 클래스 적용 방식 결정 필요
- **이슈-03 (LOW)**: Next.js 프로젝트라면 `<a>` 대신 `<Link>` 사용 권장 — dev-output.md에 이미 명시됨
